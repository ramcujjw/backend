<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/style.css"> <!-- Corrected path for the CSS file -->
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-success">
        <div class="container-fluid">
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <a class="navbar-brand" href="#"><b>EMPLOYEE APP</b></a>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/employee"><b>HOME</b></a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" href="/employee/addemployee"><b>ADD EMPLOYEE</b></a> <!-- Updated link to Add Employee -->
              </li> 
            </ul>
          </div>
        </div>
    </nav> 

    <div class="container mt-4">
      <% if (data.length > 0) { %>
        <div class="row row-cols-1 mb-0 row-cols-md-3 g-4">
          <% data.forEach(employee => { %> <!-- Using forEach for cleaner syntax -->
            <div class="col">
              <div class="card h-100">
                <div class="card-body bg-info-subtle">
                  <h5 class="card-title"><%= employee.EmployeeName %></h5><br>
                  <p class="card-text"><b>Designation: </b><%= employee.EmployeeDesignation %></p>
                  <p class="card-text"><b>Location:</b> <%= employee.EmployeeLocation %></p>
                  <p class="card-text"><b>Salary:</b> <%= employee.Salary %></p>
                  <!-- Update Form (simulates PUT request) -->
                  <form action="/employee/edit/<%= employee._id %>?_method=PUT" method="POST">
                    <button type="submit" class="btn btn-primary">Update</button>
                </form>
                
                <!-- Delete Form (simulates DELETE request) -->
                <form action="/employee/delete/<%= employee._id %>?_method=DELETE" method="POST" style="margin-top: 10px;">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
        
                </div>
              </div> 
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <p>No employees found.</p>
      <% } %>
    </div>
</body>
</html>
